# Output receiving surfaces

[Before / after at native capture size](output-receiving-surfaces-123.png). Before assets: `c69edcdfb473482b8c337f0fc45c203c6b48342b`.

Both columns use the existing `housing:NAME` scene, default micro zoom, zero ticks, and an unscaled 640 × 640 crop centred on the housing. The layout matches [the housing calibration sheet](machine-housings-calibration-67.md): before on the left, after on the right, one labelled row per machine. Before captures precede the generated texture changes.

The three briefs describe continuous ivory ceramic receiving surfaces with blue-green inlay and ornamental brass. Output scaffold cells now denote body coverage, preserving every footprint coordinate and the simulation's acceptance behavior. The existing silhouette registration handles the complete footprint with one translation and one uniform scale. No second capture path, altered critic rubric, relaxed threshold, hand-edited provenance hash or third-party image is part of this change.

The existing generator authors captions, paints candidates, measures and judges them, selects the keep, and derives its normal, relights and rig maps. Candidate histories and exact image inputs remain beside each output. The registration regression checks all three output sizes, including a shifted and scaled solid surface, against the unchanged hard gates and full footprint area.

| Output | Kept candidate | Critic | Outside ≤ 0.05 | Palette ≤ 0.35 | Off-centre ≤ 0.1 |
|---|---|---:|---:|---:|---:|
| output-1 | output-1-6 | 5/10 | 0.043 | 0.115 | 0.000 |
| output-2 | output-2-7 | 7/10 | 0.049 | 0.207 | 0.000 |
| output-3 | output-3-2 | 7/10 | 0.014 | 0.071 | 0.000 |

All three keeps pass the compound gate. Scores below 8 are selections at the unchanged three-round cap. Every output has twelve generated candidates; all 36 candidates and all three kept albedo/normal pairs were visually inspected. There are no required atom-seat landmarks on a receiving surface. Each computed-light calibration has a maximum direction error of 0.020° and sphere-shape error of 7.094°, below the unchanged 25° limit.

Read-only review, round one:

- Correctness: output footprint coordinates and simulation behavior are preserved; body registration uses uniform scale, and the selected maps retain pipeline-written provenance. The critic and hard-gate limits are unchanged.
- Premise: the art scaffold no longer translates acceptance slots into mandatory holes. The existing generator and capture scenes supply the entire result.
- Taste: the kept surfaces are broad closed ceramic bodies, with shallow relief or mineral inlay and generous perimeter fittings. The small output's 5/10 score remains visible in the record; the selection policy is unchanged.

No semantic changes resulted from these reviews, so no second round was needed.
